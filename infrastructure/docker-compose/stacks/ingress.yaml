version: '3.5'

services:

  ingress:
    image: wemakeservices/caddy-gen:latest
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
