version: '3.5'

services:

  traefik:
    image: wemakeservices/caddy-gen:latest
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
